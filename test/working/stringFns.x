entry function<> -> u32
{
    create word <- "abcdefg";
    print_(substr(word, 2, 5));

    /* WIP below */
    /*
    forward strapp function<p32 src, char letter> -> string;
    print_(strapp($(word), 'h'));
    */
}